FROM golang:1.26.1-alpine

RUN apk add --no-cache \
    build-base \
    pkgconf \
    mesa-dev \
    libx11-dev \
    libxcursor-dev \
    libxrandr-dev \
    libxinerama-dev \
    libxi-dev \
    libxxf86vm-dev \
    docker-cli

WORKDIR /src
ENTRYPOINT ["go"]
